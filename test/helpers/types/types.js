const deploy = {
  SEEDFACTORY: "SeedFactory",
  SEED: "Seed",
};

const types = {
  SEED_DEPLOY_INSTANCE: 0,
  SEED_INITIALIZE: 1,
  SEEDFACTORY_DEPLOY_INSTANCE: 2,
  SEEDFACTORY_DEPLOY_SEED: 3,
  SEED_CHANGE_CLASS: 4,
};

const classTypes = {
  CLASS_1: 0,
  CLASS_2: 1,
};

module.exports = {
  deploy,
  types,
  classTypes,
};
